syntax = "proto2";

enum MessageType {
    // Log Print
    LOG_PRINT = 1;

    // CLI Data
    CLI_DATA = 2;    

    // Add datapoint to some plot
    ADD_TO_PLOT = 3;

    // Send a bunch of data to draw an entire plot
    DRAW_PLOT = 4;
}